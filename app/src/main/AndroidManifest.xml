<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.videodownloader.period_tracker">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Period_Tracker"
        tools:targetApi="31">
        <activity
            android:name=".activity.Last_Period_StartActivity"
            android:exported="false" />
        <activity
            android:name=".activity.Period_lengthActivity"
            android:exported="false" />
        <activity
            android:name=".activity.CycleCalaulationaActivity"
            android:exported="false" />
        <activity
            android:name=".activity.InformationActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>